const { Router } = require("express");

const app = new Router()

//archived list's
app.get('/archived/:board', (req,res) => {

})

app.post('/', (req,res) => {

})

app.delete('/:id', (req,res) => {

})